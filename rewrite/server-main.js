const WebSocketServer = require('./server.js');

// Configuration options
const config = {
    server: {
        port: 1337,
        host: '127.0.0.1'
    },
    database: {
        host: '127.0